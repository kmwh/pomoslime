import 'package:flutter/material.dart';

class PremiumItemPopup extends StatelessWidget {
  const PremiumItemPopup({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
